# Monodrive Stdio Test

This diagnostic plugin runs a bundled local MCP server through stdio. It does
not use the production Monodrive MCP endpoint or OAuth.

Use the `hello-world` skill or call the `hello` MCP tool. A successful call
returns `Hello from Monodrive stdio.`
