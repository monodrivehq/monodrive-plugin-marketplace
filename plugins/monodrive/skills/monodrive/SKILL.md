---
name: monodrive
description: "Monodrive is the always-on context storage and retrieval layer, the shared company Brain for the user's work and life. Use whenever a task may benefit from context the Brain holds, high-value context should persist across sessions, or Monodrive needs setup, maintenance, or ingestion work. Trigger even when Monodrive is not named if existing context could help or new context is worth preserving. Keep signal high and avoid transient, duplicate, speculative, or low-value information."
user-invocable: false
---

Use the Monodrive MCP as the core interface. Call `guides_get` to load the relevant current workflow, then follow that guide as the source of truth. Preserve rich, durable, accurate context with a high signal-to-noise ratio. If the MCP is unavailable, help the user connect it.
